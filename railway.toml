[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma db push --skip-generate && node prisma/seed.js && node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
